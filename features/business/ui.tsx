'use client'

/**
 * Shared styled-component kit + helpers for the Business Features UI
 * (BU-01..BU-07).
 *
 * This now re-exports the shared Dashboard design system so every business
 * surface — directory, profile, owner dashboard, impact — matches the creator
 * `/dashboard` look (cream canvas, Syne / DM Sans / DM Mono type, warm-amber +
 * blue accents, white cards with a soft blue hover glow). Because the volunteer
 * and analytics UI kits re-export from here, they inherit the same system.
 */

export * from '@/features/dashboardUI'
